
<%
=begin%>
 <%
=begin%>
 <p> <%= notice %></p> 
<%
=end%> 
<%
=end%>
<h1 class="text-3xl font-bold mb-4"> All Students </h1>

<ul class="space-y-2">
    <% @students.each do |student| %>
        <li> 
            <div class="text-blue-600 haover:underline"> 
                <%= link_to student.name, student_path(student) %> 
            </div>
        </li>
    <% end %>
</ul>

<%= link_to "Add New Student", new_student_path, class: "bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded" %>




